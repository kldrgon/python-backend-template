"""跨上下文编排（应用级 Saga / Orchestration）

职责：
  - 存放需要协调多个有界上下文的复杂工作流（Saga）
  - 单个上下文内部的工作流放在各自上下文的 event_handler/ 目录

当前状态：
  - 各上下文目前通过 Choreography（事件编排）自治协作，无需集中式 Saga
  - 当某个跨上下文流程需要补偿回滚，或涉及 3 个以上上下文时，在此新增 Workflow

拆分为独立服务时：
  - 本模块可演变为独立的"编排服务"，只需修改 Activity 实现（HTTP/gRPC 替换本地调用）
"""

__all__ = []

