---
description: 设计稿约束与最小覆盖要求
globs:
  - docs/设计文档/**/*.md
  - DEVELOP_TEMP/**/*.md
alwaysApply: false
---

# Design Doc Conventions

- 设计稿先写目标、范围、业务边界，再写实现细节。
- 设计决策以界限上下文为单位，不把“是否采用领域模型”写成整个需求的一刀切结论。
- 同一界限上下文内部不混用两套主实现策略。
- 设计稿必须显式区分：已确认、假设、待确认。
- 如果边界未定，不直接下实现结论。
- 如果采用轻量实现，也要说明职责归属、依赖方向和简化理由。
- 如果存在“当前先简化、后续再升级”，必须写清升级触发条件。
