---
description: 全局硬约束，冷启动也应生效
alwaysApply: true
---

# Workspace Hard Constraints

- 先读后改：改动前先读取目标文件与直接依赖，不凭记忆直接生成整段代码。
- 默认最小改动：只改当前任务范围，不顺手重构无关代码。
- 不重复定义：同一文件禁止重复声明同名类、函数、类型别名、常量。
- 不确定先确认：不清楚是保留旧结构还是替换旧结构时，先向用户确认。
- 约束冲突先提示：若用户要求与项目规则冲突，先指出冲突点再继续。
- 输出语言默认简体中文。
