---
description: 编辑安全规则，防止冷启动失控追加
alwaysApply: true
---

# Editing Safety

- 修改已有文件时，默认在原结构上补丁式修改，不追加第二套实现。
- 若发现同一文件已有等价模型或接口，优先复用，不新建并行版本。
- 文本替换优先保持 UTF-8 与原文件风格，不引入无关格式变化。
- 涉及 DB、事件、事务、handler 加载语义时，先回读相关文件再改。
- 任何新增文件都必须与当前任务直接相关，避免生成临时垃圾文件。
